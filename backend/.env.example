DATABASE_URL="postgresql://tfa:tfa_dev_password_change_me@localhost:5432/tfa?schema=public"
PORT=3001
JWT_SECRET="replace-with-a-long-random-development-secret"
APP_URL="http://localhost:3000"
PASSWORD_RESET_TOKEN_TTL_MINUTES=30
EMAIL_VERIFICATION_TOKEN_TTL_MINUTES=1440

# Optional SMTP delivery. Without these values, verification/reset links are logged for local development.
SMTP_HOST=""
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=""
SMTP_PASSWORD=""
SMTP_FROM=""
